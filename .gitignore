# deps
node_modules
.pnpm-store

# build
.next
.turbo
dist
out
build

# env
.env
.env*.local
*.err
!.env.example

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# tsbuild
*.tsbuildinfo

# testing
coverage
playwright-report
test-results
.vitest-cache

# prisma dev
prisma/dev.db*
prisma/migrations/dev

# misc
.DS_Store
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
*.swp
*.swo

# agent / e2e tooling state (not source)
.omc/
.playwright-mcp/

# agent / e2e tooling state (not source)
.omc/
.playwright-mcp/

# local runtime artifacts
.next-pid.txt
.qt-screenshots/
server.log.err

# docker volumes (local)
docker-data/

# local runtime artifacts
backups/

# per-server deploy record (untracked, 保留在服务端)
docs/*部署记录*.md

# pnpm 11 auto-generated
pnpm-workspace.yaml

# legacy migration archive — 只 ignore 大文件 (SQL dump / CSV 兜底), 保留 README + 报告
ops/legacy/*.sql
ops/legacy/csv/
ops/legacy/*-inspect.json

# 上会话 superpowers 临时目录
docs/superpowers/

# 但保留 ops/legacy/ 下的 README 和 reports/ (重要的文档/报告)
!ops/legacy/README.md
!ops/legacy/reports/
