diff --git a/node_modules/@ant-design/pro-components/es/utils/nanoid/index.js b/node_modules/@ant-design/pro-components/es/utils/nanoid/index.js
index 36d6840..b3b8759 100644
--- a/node_modules/@ant-design/pro-components/es/utils/nanoid/index.js
+++ b/node_modules/@ant-design/pro-components/es/utils/nanoid/index.js
@@ -13,9 +13,11 @@ let genNanoid = (t = 21) => {
 
 /**
  * @description 生成uuid，如果不支持 randomUUID，就用 genNanoid
+ * PATCHED for SSR: original used `window?.` which still throws
+ * ReferenceError on the server when `window` is an undeclared identifier.
  */
 export const nanoid = () => {
-  if (window?.crypto?.randomUUID && typeof crypto.randomUUID === 'function') {
+  if (typeof window !== 'undefined' && window.crypto && window.crypto.randomUUID && typeof crypto.randomUUID === 'function') {
     return crypto.randomUUID();
   }
   return genNanoid();
